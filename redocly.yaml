apis:
  main:
    root: api/openapi.yaml

rules:
  info-license: off
  # No auth implemented yet — tracked as a known gap
  security-defined: off
  # Health endpoint intentionally has no 4xx responses
  operation-4xx-response: warn
  operation-operationId: warn
